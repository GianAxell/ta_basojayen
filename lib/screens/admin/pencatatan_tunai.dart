import 'package:flutter/material.dart';
class AdminCashRecordScreen extends StatelessWidget {
  const AdminCashRecordScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Pencatatan Tunai')));
  }
}