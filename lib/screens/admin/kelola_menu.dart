import 'package:flutter/material.dart';
class AdminKelolaMenuScreen extends StatelessWidget {
  const AdminKelolaMenuScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Kelola Menu Baso')));
  }
}